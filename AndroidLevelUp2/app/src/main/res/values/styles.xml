<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/daftGreenLight</item>
        <item name="colorPrimaryDark">@color/daftGreenDark</item>
        <item name="colorAccent">@color/daftBlue</item>
        <item name="android:windowBackground">@color/daftGray</item>
        <item name="buttonStyle">@style/Widget.AppCompat.Button.Colored</item>
    </style>

</resources>
